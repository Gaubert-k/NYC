{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "Rows Read by Layer",
      "type": "timeseries",
      "targets": [
        { "expr": "pipeline_rows_read", "legendFormat": "{{layer}} - {{operation}}" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ms" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "Pipeline Duration (ms)",
      "type": "timeseries",
      "targets": [
        { "expr": "pipeline_duration_ms", "legendFormat": "{{layer}} - {{operation}}" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "percentunit" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "title": "Null Rate Silver",
      "type": "timeseries",
      "targets": [
        { "expr": "pipeline_null_rate{layer=\"silver\"}", "legendFormat": "{{operation}}" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "title": "Duplicates Removed",
      "type": "timeseries",
      "targets": [
        { "expr": "pipeline_duplicates_removed", "legendFormat": "{{layer}} - {{operation}}" }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["nyc-taxi", "lakehouse"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "NYC Taxi Pipeline Monitoring",
  "uid": "nyc-taxi-pipeline",
  "version": 1
}
